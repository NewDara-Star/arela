
  info:
    description: API contract for activate slice
    title: Activate API
    version: 1.0.0
  openapi: 3.0.0
  paths:
    /:stack_id/activate:
      post:
        description: Endpoint defined at line 388
        parameters:
          - 
            description: The stack_id
            in: path
            name: stack_id
            required: true
            schema:
              type: string
        requestBody:
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
        responses:
          200:
            content:
              application/json:
                schema:
                  additionalProperties: true
                  type: object
            description: Successful response
          400:
            description: Bad Request
          404:
            description: Not Found
          500:
            description: Internal Server Error
        summary: Create activate
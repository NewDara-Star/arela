# Translation Slice 🗣️

## Purpose
The "Vibecoding" interface. It bridges the gap between high-level intent ("Make it pop") and low-level code ("Edit CSS").

## Core Logic
- **Input:** User natural language.
- **Engine:** OpenAI gpt-4o-mini (Architect Persona).
- **Output:** structured JSON plan.

## Dependencies
- `OPENAI_API_KEY` in `.env`.
